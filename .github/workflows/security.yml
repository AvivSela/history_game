name: Security Audit

on:
  schedule:
    # Run security audit weekly on Sundays at 2 AM UTC
    - cron: '0 2 * * 0'
  pull_request:
    branches: [ master, main, develop ]
  workflow_dispatch:

env:
  NODE_VERSION: '24'

jobs:
  dependency-review:
    name: Dependency Review
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Dependency Review
        uses: actions/dependency-review-action@v4
        with:
          fail-on-severity: moderate
          allow-licenses: MIT, Apache-2.0, BSD-2-Clause, BSD-3-Clause, ISC, 0BSD, CC0-1.0, Unlicense

  secret-scanning:
    name: Secret Scanning
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run TruffleHog
        uses: trufflesecurity/trufflehog@main
        with:
          path: ./
          base: ${{ github.event.pull_request.base.sha }}
          head: ${{ github.event.pull_request.head.sha }}
          extra_args: --only-verified

  security-audit:
    name: Security Audit
    runs-on: ubuntu-latest
    needs: [dependency-review, secret-scanning]
    if: always() && (needs.dependency-review.result == 'success' || needs.dependency-review.result == 'skipped') && (needs.secret-scanning.result == 'success' || needs.secret-scanning.result == 'skipped')
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'yarn'

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Run security audit
        id: audit
        run: yarn audit --level moderate
        continue-on-error: true

      - name: Check for outdated dependencies
        id: outdated
        run: |
          echo "🔍 Checking for outdated dependencies..."
          yarn outdated
          echo ""
          echo "⚠️  NOTE: If outdated packages were found above, consider updating them when convenient."
          echo "   Major updates (red) may include breaking changes."
          echo "   Minor updates (yellow) add new features."
          echo "   Patch updates (green) are safe to apply."
        continue-on-error: true

      - name: Security Summary
        run: |
          echo "## 🔒 Security Audit Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          # Check audit step result
          if [ "${{ steps.audit.outcome }}" == "success" ]; then
            echo "✅ **Vulnerability Scan**: No moderate or higher vulnerabilities found" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ **Vulnerability Scan**: Moderate or higher vulnerabilities detected" >> $GITHUB_STEP_SUMMARY
          fi
          
          # Check outdated step result
          if [ "${{ steps.outdated.outcome }}" == "success" ]; then
            echo "✅ **Dependency Check**: All dependencies are up to date" >> $GITHUB_STEP_SUMMARY
          else
            echo "⚠️ **Dependency Check**: Outdated dependencies found" >> $GITHUB_STEP_SUMMARY
          fi
          
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "📋 **Next Steps**: Review the logs above for:" >> $GITHUB_STEP_SUMMARY
          echo "- 🔴 **High/Critical vulnerabilities** (must fix)" >> $GITHUB_STEP_SUMMARY
          echo "- 🟡 **Moderate vulnerabilities** (should fix)" >> $GITHUB_STEP_SUMMARY
          echo "- 📦 **Outdated dependencies** (consider updating)" >> $GITHUB_STEP_SUMMARY
          echo "- 🔐 **Secrets in code** (check secret scanning results)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "💡 **Tip**: Check the 'Check for outdated dependencies' step above for package updates." >> $GITHUB_STEP_SUMMARY
        if: always()

      - name: Fail on vulnerabilities
        if: steps.audit.outcome == 'failure'
        run: |
          echo "❌ Security audit failed! Please fix the vulnerabilities above before merging."
          exit 1

 